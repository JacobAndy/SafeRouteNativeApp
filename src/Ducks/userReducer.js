const initialState = {
  signedIn: false
};

//CONSTANTS

//REDUCER FUNCTION
export default function userReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return { ...state };
  }
}

//ACTION CREATORS
